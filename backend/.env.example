# --- LLM Provider ---
# Set to "local" during development (uses Ollama, no API key needed)
# Set to "claude" for production (requires ANTHROPIC_API_KEY)
LLM_PROVIDER=local

# --- Anthropic (only needed when LLM_PROVIDER=claude) ---
ANTHROPIC_API_KEY=your_key_here

# --- Ollama (only needed when LLM_PROVIDER=local) ---
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=gemma3:1b

# --- RAG / ChromaDB ---
CHROMA_PERSIST_DIR=./chroma_db
EMBEDDING_MODEL=all-MiniLM-L6-v2
TOP_K=6

# --- Server ---
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
